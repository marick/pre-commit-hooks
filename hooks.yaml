-   id: only-branch-pushes
    name: only branch pushes
    description: prevent pushes to a list of forbidden branches
    entry: hooks/only-branch-pushes
    language: script
    files: ''
-   id: prohibit-suspicious-patterns
    name: prohibit suspicious patterns
    description: prevent commits of files with contents matching suspicious patterns
    entry: hooks/prohibit-suspicious-patterns
    language: script
    files: ''
-   id: prohibit-suspicious-files
    name: prohibit files that shouldn't be checked in
    description: prevent commits of files with names matching suspicious patterns
    entry: hooks/prohibit-suspicious-files
    language: script
    files: ''
